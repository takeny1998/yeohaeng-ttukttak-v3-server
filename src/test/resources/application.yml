spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:yeohaeng_ttukttak_v3;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: validate

    properties:
      hibernate:
        highlight_sql: true

  flyway:
    enabled: true

kakao.map.api-key: kakao-api-key

bayesian:
  entire-average: 0.0
  coefficient: 0.0